

// function superbowlWin(record) {
//     let win = record.find(result => record.result === "W")
//     if (win === true) {
//         record.year
//     } else {
//         undefined
//     }
// }


// function superbowlWin(record) {
//     for (const team of record) {
//         if (record.find(result => record.result === "W")) {
//             return record.year
//         } else {
//             return undefined
//         }
//     }
// }
// superbowlWin(record)

// function superbowlWin(record)
// return record.result === "W"

// console.log(record.find("W").year)



function superbowlWin(array) {
    const win = record.find(superbowlWin).year;
    return array.result === "W";
}